import 'package:flutter/cupertino.dart';

class PaddingConstants {
  static EdgeInsets low_pad = const EdgeInsets.all(5);
  static EdgeInsets med_pad = const EdgeInsets.all(10);
  static EdgeInsets high_pad = const EdgeInsets.all(20);
  static EdgeInsets extreme_pad = const EdgeInsets.all(60);
}
